#!/usr/bin/env bash
# ============================================================================
# SCAFFOLD — REPLACE THIS FILE.
#
# This is not a working runner. Reviewers re-run your submission end to end:
# they overlay this folder (plus your fixed tracking/config/) on a clean
# canonical checkout and execute:   bash submission/run.sh
#
# Your replacement must:
#   * be python3-stdlib-only (no pip installs, no third-party packages)
#   * use no network beyond the loopback lab API (http://127.0.0.1:8781)
#   * run from the repository root
#   * regenerate submission/answers.json, submission/decisions.json and
#     submission/plan.json — and submission/pull_proof.json (pulling from
#     the loopback API inside your script is fine)
#   * exit non-zero on failure
#
# Hand-computed numbers with no working script score zero. Keep helper
# scripts under submission/; run.sh is the entrypoint.
# ============================================================================
set -euo pipefail

echo "submission/run.sh is still the scaffold."
echo "Replace it with your own runner that regenerates answers.json, decisions.json"
echo "and plan.json (and pull_proof.json) from the lab data — python3 stdlib only."
exit 1
